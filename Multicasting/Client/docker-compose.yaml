version: '3.9'

services:
  client:
    build:
      context: .         # Dockerfile is in same folder
      dockerfile: Dockerfile
    image: client:latest
    container_name: client_app
    ports:
      - "8086:86"
    environment:
      DOTNET_ENVIRONMENT: Development
    restart: unless-stopped
